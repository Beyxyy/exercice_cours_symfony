{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
    {% for product in products %}
        <h1>{{ product.description }}</h1>
        <h2>vendu par {{ product.seller.title }}</h2>

    {% endfor %}
{% endblock %}
